1- Alterar parâmetro 'scriptSql' do sql de cadastro para o sql com o novo usuário a ser cadastrado
2- Instale biblioteca pg do node com 'npm install pg'
3- Executar o script com comando 'node sql.js'

estilo json
[
    {
        "url": "jdbc:postgresql://host:port/sid",
        "user": "user",
        "password": "password"
    }
]